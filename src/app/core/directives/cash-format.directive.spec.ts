import { CashFormatDirective } from './cash-format.directive';

describe('CashFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new CashFormatDirective();
    expect(directive).toBeTruthy();
  });
});
