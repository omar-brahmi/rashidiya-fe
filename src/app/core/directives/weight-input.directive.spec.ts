import { WeightInputDirective } from './weight-input.directive';

describe('WeightInputDirective', () => {
  it('should create an instance', () => {
    const directive = new WeightInputDirective();
    expect(directive).toBeTruthy();
  });
});
