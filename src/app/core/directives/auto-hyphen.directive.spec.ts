import { AutoHyphenDirective } from './auto-hyphen.directive';

describe('AutoHyphenDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoHyphenDirective();
    expect(directive).toBeTruthy();
  });
});
